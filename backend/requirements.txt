# backend/requirements.txt — Python dependencies for the FastAPI backend
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0          # async registry calls (Person B)
rapidfuzz>=3.9.0       # typosquat similarity (Person B)
pydantic>=2.7.0        # data validation (bundled with FastAPI 0.111+)
